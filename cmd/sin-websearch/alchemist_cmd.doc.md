# alchemist_cmd.go

CLI commands for running, initializing, reporting, and viewing the history of autonomous Alchemist experiment loops.

Dependencies: standard library (`context`, `fmt`, `os`, `os/signal`, `path/filepath`, `syscall`, `time`), and `github.com/OpenSIN-Code/web_search_bundle/internal/alchemist`.
