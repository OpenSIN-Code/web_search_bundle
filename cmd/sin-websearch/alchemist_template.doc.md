Default `program.md` templates for the alchemist `init` command.

- `getProgramTemplate` returns a Go or Python/ML template string.
- Templates include the target file, metric, rules, hypothesis queue, and learnings section.

Related files: `alchemist_init_cmd.go`, `internal/alchemist/program_md.go`.
