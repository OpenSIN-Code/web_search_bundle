# server.go

MCP server exposing sin-websearch tools to compatible agents via stdio.

Dependencies: `internal/orchestrator`, `internal/engines`, `internal/briefing`, `internal/prompts`, `internal/sidecar`.
