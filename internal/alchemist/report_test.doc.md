Tests for alchemist report rendering and helpers.

- Covers `MorningReport.RenderMarkdown`, `RecentN`, `toFloat`, `shortSHA`, and `renderSimple`.
- Uses minimal in-memory report structures; no filesystem or network calls.

Related files: `report.go`, `report_test.go`.
