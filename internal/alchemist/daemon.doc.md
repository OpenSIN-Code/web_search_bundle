# daemon.go

Autonomous research daemon loop logic implementing Karpathy-style optimization.

Dependencies: standard library (`context`, `log/slog`, `os`, `path/filepath`, `sync`, `time`), and `internal/experiment`.
