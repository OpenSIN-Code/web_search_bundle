# history.go

Manages SQLite persistent storage for Alchemist experiment runs, including summaries and recent history tracking.

Dependencies: standard library (`database/sql`, `fmt`, `os`, `path/filepath`, `time`), and `modernc.org/sqlite`.
