# orchestrator.go

Coordinates parallel fan-out searches across multiple engines, then applies entity resolution, humor scoring, and story clustering.

Dependencies: `internal/engines`, `internal/resolver`, `internal/judge`, `internal/clustering`.
