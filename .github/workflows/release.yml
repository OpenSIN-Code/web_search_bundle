name: Release

on:
  push:
    tags:
      - 'v*'
      - '!v*-ceo-audit-v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Build binaries
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir -p dist
          LDFLAGS="-s -w -X github.com/OpenSIN-Code/web_search_bundle/internal/config.Version=${VERSION} -X main.commit=${GITHUB_SHA} -X main.date=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          GOOS=darwin GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o dist/sin-websearch-darwin-amd64 ./cmd/sin-websearch
          GOOS=darwin GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o dist/sin-websearch-darwin-arm64 ./cmd/sin-websearch
          GOOS=linux GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o dist/sin-websearch-linux-amd64 ./cmd/sin-websearch
          GOOS=linux GOARCH=arm64 go build -ldflags="${LDFLAGS}" -o dist/sin-websearch-linux-arm64 ./cmd/sin-websearch
          GOOS=windows GOARCH=amd64 go build -ldflags="${LDFLAGS}" -o dist/sin-websearch-windows-amd64.exe ./cmd/sin-websearch

      - name: Create checksums
        run: |
          cd dist
          sha256sum * > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
